{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jesus-mata/tanugate/internal/config/gateway-config",
  "$defs": {
    "APIKeyConfig": {
      "properties": {
        "header": {
          "type": "string"
        },
        "keys": {
          "items": {
            "$ref": "#/$defs/APIKeyEntry"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "APIKeyEntry": {
      "properties": {
        "key": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "AuthProvider": {
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "jwt",
            "apikey",
            "oidc"
          ],
          "description": "Authentication provider type"
        },
        "jwt": {
          "$ref": "#/$defs/JWTConfig",
          "description": "JWT authentication settings"
        },
        "api_key": {
          "$ref": "#/$defs/APIKeyConfig",
          "description": "API key authentication settings"
        },
        "oidc": {
          "$ref": "#/$defs/OIDCConfig",
          "description": "OpenID Connect settings"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "type"
      ]
    },
    "CircuitBreakerConfig": {
      "properties": {
        "failure_threshold": {
          "type": "integer",
          "minimum": 1,
          "description": "Failures before opening the circuit"
        },
        "success_threshold": {
          "type": "integer",
          "minimum": 1,
          "description": "Successes in half-open state before closing"
        },
        "timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Time to wait before transitioning to half-open (e.g. 30s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "JWTConfig": {
      "properties": {
        "secret": {
          "type": "string",
          "description": "HMAC shared secret"
        },
        "public_key_file": {
          "type": "string",
          "description": "Path to RSA/ECDSA public key file"
        },
        "algorithm": {
          "type": "string",
          "enum": [
            "HS256",
            "HS384",
            "HS512",
            "RS256",
            "RS384",
            "RS512",
            "ES256",
            "ES384",
            "ES512"
          ],
          "description": "JWT signing algorithm"
        },
        "issuer": {
          "type": "string",
          "description": "Expected token issuer"
        },
        "audience": {
          "type": "string",
          "description": "Expected token audience"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "LoggingConfig": {
      "properties": {
        "level": {
          "type": "string",
          "enum": [
            "debug",
            "info",
            "warn",
            "error"
          ],
          "description": "Log level",
          "default": "info"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "MatchConfig": {
      "properties": {
        "path_regex": {
          "type": "string"
        },
        "methods": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path_regex"
      ]
    },
    "MiddlewareDefinition": {
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "cors",
            "rate_limit",
            "auth",
            "transform"
          ]
        },
        "config": true
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "type"
      ]
    },
    "MiddlewareRef": {
      "properties": {
        "ref": {
          "type": "string"
        },
        "config": true
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ref"
      ]
    },
    "OIDCConfig": {
      "properties": {
        "issuer_url": {
          "type": "string"
        },
        "jwks_url": {
          "type": "string"
        },
        "introspection_url": {
          "type": "string"
        },
        "client_id": {
          "type": "string"
        },
        "client_secret": {
          "type": "string"
        },
        "audience": {
          "type": "string"
        },
        "allowed_algorithms": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "skip_issuer_check": {
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "RateLimitGlobalConfig": {
      "properties": {
        "backend": {
          "type": "string",
          "enum": [
            "memory",
            "redis"
          ],
          "description": "Rate limit storage backend",
          "default": "memory"
        },
        "redis": {
          "$ref": "#/$defs/RedisConfig",
          "description": "Redis connection settings (required when backend is redis)"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "RedisConfig": {
      "properties": {
        "addr": {
          "type": "string",
          "description": "Redis address (host:port)"
        },
        "password": {
          "type": "string",
          "description": "Redis password"
        },
        "db": {
          "type": "integer",
          "minimum": 0,
          "description": "Redis database number"
        },
        "pool_size": {
          "type": "integer",
          "minimum": 0,
          "description": "Connection pool size",
          "default": 10
        },
        "min_idle_conns": {
          "type": "integer",
          "minimum": 0,
          "description": "Minimum idle connections"
        },
        "dial_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Connection dial timeout (e.g. 5s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "read_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Read timeout (e.g. 3s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "write_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Write timeout (e.g. 3s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "max_retries": {
          "type": "integer",
          "minimum": 0,
          "description": "Max retries on failure"
        },
        "query_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Query timeout (e.g. 100ms)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "tls_enabled": {
          "type": "boolean",
          "description": "Enable TLS for Redis connection"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "addr"
      ]
    },
    "RetryConfig": {
      "properties": {
        "max_retries": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of retry attempts"
        },
        "initial_delay": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Initial delay before first retry (e.g. 100ms)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "multiplier": {
          "type": "number",
          "minimum": 1,
          "description": "Backoff multiplier for subsequent retries"
        },
        "retryable_status_codes": {
          "items": {
            "type": "integer"
          },
          "type": "array",
          "description": "HTTP status codes that trigger a retry"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "RouteConfig": {
      "properties": {
        "name": {
          "type": "string"
        },
        "match": {
          "$ref": "#/$defs/MatchConfig"
        },
        "upstream": {
          "$ref": "#/$defs/UpstreamConfig"
        },
        "retry": {
          "$ref": "#/$defs/RetryConfig"
        },
        "circuit_breaker": {
          "$ref": "#/$defs/CircuitBreakerConfig"
        },
        "skip_middlewares": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Names of global middlewares to skip for this route"
        },
        "middlewares": {
          "items": {
            "$ref": "#/$defs/MiddlewareRef"
          },
          "type": "array",
          "description": "Ordered middleware refs for this route"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "match",
        "upstream"
      ]
    },
    "ServerConfig": {
      "properties": {
        "host": {
          "type": "string",
          "description": "Bind address",
          "default": "0.0.0.0"
        },
        "port": {
          "type": "integer",
          "maximum": 65535,
          "minimum": 1,
          "description": "Listen port",
          "default": 8080
        },
        "read_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Max time to read the entire request (e.g. 30s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "write_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Max time to write the response (e.g. 30s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "idle_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Max time for idle keep-alive connections (e.g. 120s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "shutdown_timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "description": "Graceful shutdown timeout (e.g. 15s)",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        },
        "trusted_proxies": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "CIDR ranges or IPs of trusted reverse proxies"
        },
        "max_header_bytes": {
          "type": "integer",
          "minimum": 4096,
          "description": "Maximum size of request headers in bytes",
          "default": 1048576
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "UpstreamConfig": {
      "properties": {
        "url": {
          "type": "string"
        },
        "path_rewrite": {
          "type": "string"
        },
        "timeout": {
          "type": "string",
          "pattern": "^([0-9]+(ns|us|µs|ms|s|m|h))+$",
          "examples": [
            "30s",
            "1m",
            "500ms",
            "2h30m"
          ]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "url"
      ]
    }
  },
  "properties": {
    "server": {
      "$ref": "#/$defs/ServerConfig",
      "description": "HTTP server settings"
    },
    "logging": {
      "$ref": "#/$defs/LoggingConfig",
      "description": "Logging configuration"
    },
    "rate_limit": {
      "$ref": "#/$defs/RateLimitGlobalConfig",
      "description": "Global rate limiting settings"
    },
    "auth_providers": {
      "additionalProperties": {
        "$ref": "#/$defs/AuthProvider"
      },
      "type": "object",
      "description": "Named authentication providers"
    },
    "middleware_definitions": {
      "additionalProperties": {
        "$ref": "#/$defs/MiddlewareDefinition"
      },
      "type": "object",
      "description": "Reusable middleware definitions"
    },
    "middlewares": {
      "items": {
        "$ref": "#/$defs/MiddlewareRef"
      },
      "type": "array",
      "description": "Global middleware chain applied to all routes"
    },
    "routes": {
      "items": {
        "$ref": "#/$defs/RouteConfig"
      },
      "type": "array",
      "description": "Route definitions"
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": [
    "routes"
  ]
}
